# 🔧 Встановлення Terraform

## Що таке Terraform?

Terraform - це інструмент Infrastructure as Code (IaC) від HashiCorp, який дозволяє:
- Описувати інфраструктуру у вигляді коду (файли .tf)
- Автоматично створювати, змінювати та видаляти ресурси
- Відстежувати стан інфраструктури
- Версіонувати зміни інфраструктури

## Встановлення для Windows

### Варіант 1: 🚀 Через winget (Рекомендований - швидко та просто)

1. **Перевірили наявність winget**:
   ```bash
   winget --version
   # Результат: v1.12.350 ✅
   ```

2. **Встановили Terraform**:
   ```bash
   winget install Hashicorp.Terraform
   ```

3. **Що відбулося автоматично**:
   - ✅ Завантажено Terraform v1.13.5 (28.9 MB)
   - ✅ Перевірено хеш архіву
   - ✅ Розпаковано файли
   - ✅ Додано алиас "terraform"
   - ✅ Автоматично додано до PATH
   - ✅ Успішно встановлено!

4. **Перезапустіть VS Code/термінал** щоб оновити PATH

### Варіант 2: Завантаження з офіційного сайту

**Якщо winget недоступний:**

1. **Перейдіть на**: https://www.terraform.io/downloads.html

2. **Оберіть правильну версію**:
   - ✅ **AMD64** - для 64-бітних систем (рекомендовано)
   - ❌ **386** - для 32-бітних систем (застаріло)

3. **Завантажте**: `terraform_1.13.5_windows_amd64.zip`

4. **Встановлення вручну**:
   - Створіть папку `C:\terraform\`
   - Розпакуйте `terraform.exe` в цю папку
   - Додайте `C:\terraform\` до системної змінної PATH:
     - Win+R → `sysdm.cpl` → Advanced → Environment Variables
     - System Variables → Path → Edit → New → `C:\terraform\`
     - OK → OK → OK
   - Перезапустіть термінал

## Перевірка встановлення

Після встановлення перезапустіть термінал та перевірте:

```bash
terraform --version
```

Повинно показати щось на зразок:
```
Terraform v1.6.x
on windows_amd64
```

## Базові команди Terraform

```bash
# Ініціалізація проекту (завантаження провайдерів)
terraform init

# Перевірка синтаксису
terraform validate

# Планування змін (що буде створено/змінено/видалено)
terraform plan -var-file="environments/lab.tfvars"

# Застосування змін
terraform apply -var-file="environments/lab.tfvars"

# Видалення всіх ресурсів
terraform destroy

# Форматування коду
terraform fmt

# Показати поточний стан
terraform show
```

## 💡 Корисні поради:

- Завжди запускайте `terraform plan` перед `terraform apply` з вказанням environment файлу
- Використовуйте `terraform validate` для перевірки синтаксису
- Зберігайте файли `.tfstate` в безпечному місці (не в git!)
- Використовуйте версіонування для провайдерів
- **Environment файли**: Обов'язково використовуйте `-var-file="environments/lab.tfvars"` для всіх команд

---

**Наступний крок**: Після встановлення створюємо першу Terraform конфігурацію для VPC.